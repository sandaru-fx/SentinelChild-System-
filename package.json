{
    "name": "sentinel-child-system",
    "version": "1.0.0",
    "description": "Child Harassment & Abuse Reporting System",
    "scripts": {
        "install:all": "npm install && cd \"frontend\" && npm install",
        "backend": "go run .",
        "frontend": "cd \"frontend\" && npm run dev",
        "dev": "concurrently \"npm run backend\" \"npm run frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}